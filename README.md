# Docker Hosting for multipe domains
This docker container is meant to easily deploy a nginx docker container to allow for routing of domains to multiple docker builds/containers.
Primarly for hobbist who wants to limit amount of servers used.
